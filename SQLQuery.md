### To select records from Table "User"
`SELECT * FROM "User" `

### To delete from Table "User"
`DELETE FROM "User" `

### To reset primary key to begining for Tbale "User"
` ALTER SEQUENCE "User_id_seq" RESTART WITH 1`